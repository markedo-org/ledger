# Security

Report vulnerabilities to lg@markedo.com. Do not open a public issue for a
live secret or a remote-code path.

This process stores bearer tokens hashed. Treat a minted token as a password.
Do not commit `.env`, `ledger.sqlite`, or a filled `LEDGER_BOOT_TOKEN`.
