# Contributing

`make test` and `make lint` before you send a change. Go 1.24+.

Read `docs/design.md` before reopening the v1 cut. The HTTP API is the system.
HTML and MCP are clients. Do not wrap the binary as stdio MCP.

Issues and pull requests are welcome on this repository.
